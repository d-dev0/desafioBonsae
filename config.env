# Porta do servidor HTTP
PORT=3000

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_DB=0

# Pasta local para armazenar relatórios
STORAGE_DIR=./storage

# Configs da fila
QUEUE_NAME=report-generation

JOB_CONCURRENCY=4
JOB_TIMEOUT_MS=600000
JOB_ATTEMPTS=3
JOB_BACKOFF_MS=15000

# Base URL pública para montar links de download
PUBLIC_BASE_URL=http://localhost:3000
# Postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=residencia
